# Model package

Owns validated, serialization-friendly state independent of rendering. Prefer immutable records and centralized bounds. Portable state may preserve media/settings/playback intent, but a placed block receives a fresh placement identity.
