# Resources

- `assets/pockettune`: language, block/item models, textures and blockstates
- `data/pockettune`: recipes, loot tables and tags
- `pack.mcmeta`: resource/data pack metadata

Names must match registry IDs exactly. User-facing stable text belongs in language JSON; generated resources belong under `src/generated/resources` and must not include generator cache files.
