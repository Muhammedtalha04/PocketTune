# Tests

Tests mirror production packages and focus on deterministic validation, state transitions, timelines, payload bounds, process registries and cancellation races. Default JUnit must not require Minecraft UI startup, internet access, `yt-dlp` or `mpv`; real smoke-test entry points remain explicit manual tasks.
